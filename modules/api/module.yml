schema_version: 1
name: api
status: interface_only
summary: >-
  Versioned public API with scoped tenant-bound credentials, bounded pagination,
  stable errors, rate limits and an OpenAPI description.
requires_approved_scope: true
conformance:
  contract: MODULE.md
  required_checks: []
